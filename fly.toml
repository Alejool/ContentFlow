app = "contenflow"
primary_region = "gru" 

[build]
  dockerfile = "Dockerfile"

[[services]]
  protocol = "tcp"
  internal_port = 8080
  processes = ["app"]

  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [[services.http_checks]]
    interval = "30s"
    method = "get"
    path = "/favicon.ico"
    timeout = "5s"
    grace_period = "1200s"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1